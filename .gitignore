# Ignore rendered cluster configs but keep the folder structure
cluster/rendered/*
!cluster/rendered/.gitignore

# Temporary decrypted secrets - NEVER commit these!
/tmp/secrets.yaml
**/secrets-decrypted.yaml
**/secrets.yaml.dec
**/secrets.yaml.tmp
**/*-secrets-*.yaml

# Other temporary files
*.tmp
*.bak
